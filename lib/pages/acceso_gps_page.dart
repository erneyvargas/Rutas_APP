import 'package:flutter/material.dart';

class AccesoGpsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Widget AccesoGps'), centerTitle: true),
      body: Center(
        child: Text('AccesoGps Page'),
      ),
    );
  }
}
